---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Arjun Basandrai — ML Engineer & Researcher"
  description="Arjun Basandrai builds machine learning systems and ships them into production. Projects in applied ML, ML systems, and research."
  current="home"
>
  <main>
    <!-- ===== HERO ===== -->
    <section class="hero wrap">
      <div class="hero-grid">
        <div>
          <div class="hero-availability"><span class="pulse"></span> Open to ML / research roles — Fall 2026</div>
          <h1>Machine learning,<br>built to be <em>understood.</em></h1>
          <p class="lead hero-sub">I'm Arjun — an ML engineer and researcher working on systems that learn from messy, real-world data. I build models end to end and ship them into production — and I spend my weekends counting birds.</p>
          <div class="hero-actions">
            <a class="btn btn--solid" href="/projects">View work <span class="arr">→</span></a>
            <a class="btn btn--ghost" href="/resume">Résumé</a>
            <a class="btn btn--ghost" href="/contact">Get in touch</a>
          </div>
        </div>
        <div>
          <div class="ph hero-portrait"><span>portrait.jpg</span></div>
          <dl class="hero-side">
            <dt>Currently</dt>
            <dd>ML Engineer @ Aviary Labs</dd>
            <dt>Based in</dt>
            <dd>Lisbon, Portugal</dd>
            <dt>Find me</dt>
            <dd><a class="txtlink" href="#">GitHub</a> · <a class="txtlink" href="#">LinkedIn</a></dd>
          </dl>
        </div>
      </div>
    </section>

    <!-- ===== MARQUEE ===== -->
    <div class="marquee" aria-hidden="true">
      <div class="marquee-track">
        <span>PyTorch <b>·</b> JAX <b>·</b> Distributed training <b>·</b> Retrieval <b>·</b> Information theory <b>·</b> Rust <b>·</b> TypeScript <b>·</b> Inference optimization <b>·</b> Bioacoustics <b>·</b> </span>
        <span>PyTorch <b>·</b> JAX <b>·</b> Distributed training <b>·</b> Retrieval <b>·</b> Information theory <b>·</b> Rust <b>·</b> TypeScript <b>·</b> Inference optimization <b>·</b> Bioacoustics <b>·</b> </span>
      </div>
    </div>

    <!-- ===== FEATURED WORK ===== -->
    <section class="section wrap">
      <div class="sect-head">
        <div>
          <span class="eyebrow">Selected work</span>
          <h2 class="sect-title reveal" style="margin-top:16px;">Things I've built<br>and studied.</h2>
        </div>
        <a class="txtlink" href="/projects" style="white-space:nowrap;">All projects →</a>
      </div>

      <div class="feat-list">
        <a class="feat reveal" href="/projects/birdnet-edge">
          <span class="feat-no">01</span>
          <span class="feat-title"><span class="kind">Applied ML</span>BirdNET-Edge</span>
          <span class="feat-desc">On-device bioacoustic classifier identifying 1,400+ species from raw audio at &lt;40ms latency on a Raspberry Pi.</span>
          <span class="feat-go"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7M17 7H8M17 7v9" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
        </a>
        <a class="feat reveal" href="/projects/birdnet-edge">
          <span class="feat-no">02</span>
          <span class="feat-title"><span class="kind">Research</span>Sparse Routing for Long-Context Retrieval</span>
          <span class="feat-desc">A learned router that cuts retrieval FLOPs by 6× while improving recall on long documents.</span>
          <span class="feat-go"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7M17 7H8M17 7v9" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
        </a>
        <a class="feat reveal" href="/projects/birdnet-edge">
          <span class="feat-no">03</span>
          <span class="feat-title"><span class="kind">Systems</span>Quill — a typed feature store</span>
          <span class="feat-desc">Schema-validated feature pipelines in Rust + TypeScript, with point-in-time correctness baked in.</span>
          <span class="feat-go"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7M17 7H8M17 7v9" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
        </a>
      </div>
    </section>

    <!-- ===== TECHNICAL FOCUS ===== -->
    <section class="section--tight wrap">
      <span class="eyebrow">What I work on</span>
      <div class="focus-grid reveal" style="margin-top:28px;">
        <div class="focus-cell">
          <span class="n">01</span>
          <h3>Machine Learning</h3>
          <p>Training and evaluating models that hold up outside the notebook — representation learning, retrieval, and sequence modeling.</p>
          <div class="skills"><span class="tag">PyTorch</span><span class="tag">JAX</span><span class="tag">Transformers</span></div>
        </div>
        <div class="focus-cell">
          <span class="n">02</span>
          <h3>ML Systems</h3>
          <p>The unglamorous half: data pipelines, feature stores, inference serving, and the observability that keeps models honest.</p>
          <div class="skills"><span class="tag">Rust</span><span class="tag">Ray</span><span class="tag">Triton</span></div>
        </div>
        <div class="focus-cell">
          <span class="n">03</span>
          <h3>Full-Stack &amp; Research</h3>
          <p>Shipping research as usable products, and turning product telemetry back into research questions worth asking.</p>
          <div class="skills"><span class="tag">TypeScript</span><span class="tag">Astro</span><span class="tag">D1</span></div>
        </div>
      </div>
    </section>

    <!-- ===== PERSONAL / FIELD LAYER ===== -->
    <section class="field section">
      <div class="wrap field-grid">
        <div class="field-photos reveal">
          <div class="ph"><span>field photo — coast</span></div>
          <div class="ph"><span>kingfisher</span></div>
          <div class="ph"><span>notebook</span></div>
        </div>
        <div class="reveal">
          <span class="eyebrow">Away from the keyboard</span>
          <h2 class="sect-title" style="margin:16px 0;">A life list, kept in the margins.</h2>
          <p class="lead">When I'm not training models, I'm usually outside trying to identify the thing that just flew past. I've logged <strong>412 species</strong> across 19 countries, and a surprising amount of my ML intuition comes from learning to tell two near-identical sparrows apart at 50 meters.</p>
          <p style="margin-top:18px;"><a class="txtlink" href="/about">More about me →</a></p>
        </div>
      </div>
    </section>

    <!-- ===== CTA ===== -->
    <section class="section wrap cta">
      <span class="eyebrow" style="justify-content:center;">Let's talk</span>
      <h2 class="display">Building something that needs to <em>learn?</em></h2>
      <div class="hero-actions" style="justify-content:center;">
        <a class="btn btn--accent" href="/contact">Start a conversation <span class="arr">→</span></a>
        <a class="btn btn--ghost" href="/resume">Download résumé</a>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .hero { padding-top: clamp(48px, 9vw, 110px); padding-bottom: clamp(40px, 6vw, 72px); position: relative; }
  .hero-grid { display: grid; grid-template-columns: 1.55fr 1fr; gap: clamp(28px, 5vw, 64px); align-items: end; }
  .hero-availability {
    font-family: var(--font-mono); font-size: 12px; letter-spacing: 0.04em; color: var(--muted);
    display: inline-flex; align-items: center; gap: 9px; margin-bottom: 26px;
  }
  .pulse { width: 8px; height: 8px; border-radius: 50%; background: var(--st-active); position: relative; }
  .pulse::after { content:""; position:absolute; inset:-4px; border-radius:50%; border:1px solid var(--st-active); animation: pulse 2.4s ease-out infinite; }
  @keyframes pulse { 0%{transform:scale(.6);opacity:.9} 100%{transform:scale(1.8);opacity:0} }
  .hero h1 { font-family: var(--font-serif); font-weight: 400; font-size: clamp(2.9rem, 8.2vw, 6.6rem); line-height: 0.94; letter-spacing: -0.025em; }
  .hero h1 em { font-style: italic; color: var(--accent-ink); }
  .hero-sub { margin-top: 26px; max-width: 50ch; }
  .hero-actions { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 32px; }
  .hero-side { font-family: var(--font-mono); font-size: 13px; color: var(--ink-soft); line-height: 1.7; }
  .hero-side dt { color: var(--muted); font-size: 11px; letter-spacing: 0.12em; text-transform: uppercase; margin-top: 18px; }
  .hero-side dt:first-child { margin-top: 0; }
  .hero-side dd { margin: 2px 0 0; }
  .hero-side a:hover { color: var(--accent-ink); }
  .hero-portrait { margin-top: 14px; aspect-ratio: 4/5; border-radius: var(--radius); }

  .marquee { border-block: 1px solid var(--line); overflow: hidden; padding-block: 16px; }
  .marquee-track { display: flex; gap: 48px; white-space: nowrap; font-family: var(--font-mono); font-size: 13px; color: var(--muted); animation: scroll 38s linear infinite; }
  .marquee-track span { display: inline-flex; gap: 48px; }
  .marquee-track b { color: var(--accent-ink); font-weight: 500; }
  @keyframes scroll { to { transform: translateX(-50%); } }

  .sect-head { display: flex; align-items: flex-end; justify-content: space-between; gap: 24px; margin-bottom: 40px; }

  /* Featured work */
  .feat-list { display: grid; gap: 0; border-top: 1px solid var(--line); }
  .feat {
    display: grid; grid-template-columns: 64px 1.5fr 1fr auto; gap: 24px; align-items: center;
    padding: 26px 8px; border-bottom: 1px solid var(--line); position: relative; transition: padding .3s;
  }
  .feat::before { content:""; position:absolute; left:0; top:0; bottom:0; width:0; background: var(--accent-soft); z-index:-1; transition: width .35s ease; }
  .feat:hover { padding-inline: 18px; }
  .feat:hover::before { width: 100%; }
  .feat-no { font-family: var(--font-mono); font-size: 13px; color: var(--faint); }
  .feat-title { font-family: var(--font-serif); font-size: clamp(1.4rem, 2.6vw, 2rem); letter-spacing: -0.015em; }
  .feat-title .kind { font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.1em; text-transform: uppercase; color: var(--accent-ink); display: block; margin-bottom: 6px; }
  .feat-desc { color: var(--muted); font-size: 14.5px; line-height: 1.55; }
  .feat-go { width: 42px; height: 42px; border-radius: 50%; border: 1px solid var(--line-strong); display: grid; place-items: center; color: var(--ink-soft); transition: all .25s; }
  .feat:hover .feat-go { background: var(--ink); color: var(--paper); border-color: var(--ink); transform: rotate(-45deg); }

  /* Focus */
  .focus-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--line); border: 1px solid var(--line); border-radius: var(--radius); overflow: hidden; }
  .focus-cell { background: var(--surface); padding: 30px 26px; transition: background .25s; }
  .focus-cell:hover { background: var(--paper-2); }
  .focus-cell .n { font-family: var(--font-mono); font-size: 12px; color: var(--accent-ink); }
  .focus-cell h3 { font-family: var(--font-serif); font-size: 1.32rem; margin: 12px 0 8px; }
  .focus-cell p { color: var(--muted); font-size: 14px; line-height: 1.55; margin: 0; }
  .focus-cell .skills { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 16px; }

  /* Personal / field layer */
  .field { background: var(--paper-2); border-block: 1px solid var(--line); }
  .field-grid { display: grid; grid-template-columns: 1fr 1.1fr; gap: clamp(28px,5vw,56px); align-items: center; }
  .field-photos { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
  .field-photos .ph:first-child { grid-row: span 2; aspect-ratio: 3/4; }
  .field-photos .ph { aspect-ratio: 4/3; }

  /* CTA */
  .cta { text-align: center; }
  .cta .display { margin: 18px auto 28px; max-width: 16ch; }

  @media (max-width: 860px) {
    .hero-grid { grid-template-columns: 1fr; align-items: start; }
    .hero-side { display: grid; grid-template-columns: 1fr 1fr; gap: 14px 28px; }
    .focus-grid { grid-template-columns: 1fr; }
    .feat { grid-template-columns: 40px 1fr auto; }
    .feat-desc, .feat .badge { display: none; }
    .field-grid { grid-template-columns: 1fr; }
  }
</style>
